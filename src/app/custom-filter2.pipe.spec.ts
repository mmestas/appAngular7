import { CustomFilter2Pipe } from './custom-filter2.pipe';

describe('CustomFilter2Pipe', () => {
  it('create an instance', () => {
    const pipe = new CustomFilter2Pipe();
    expect(pipe).toBeTruthy();
  });
});
